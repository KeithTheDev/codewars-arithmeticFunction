function arithmetic(a, b, math){
  if(math === "add"){
    return a + b;
  } else if(math === "subtract"){
    return a - b;
  } else if(math === "multiply"){
    return a * b;
  } else if(math === "divide"){
    return a / b;
  }
}
